# Daily
DAILY_API_KEY=
DAILY_API_URL=https://api.daily.co/v1

# Agent
AGENT_HOST=0.0.0.0
AGENT_PORT=7860

# Web (browser reaches agent via host-mapped port)
NEXT_PUBLIC_AGENT_URL=http://localhost:7860
# Used for absolute Open Graph / Twitter URLs
# NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Voice pipeline (required) — Sarvam STT + LLM + TTS
SARVAM_API_KEY=

# Local store (SQLite). Defaults to apps/agent/data/kubera.db.
# KUBERA_DB_PATH=

# Optional overrides
# SARVAM_LANGUAGE=en-IN
# SARVAM_VOICE=shubh
# SARVAM_TTS_MODEL=bulbul:v3
