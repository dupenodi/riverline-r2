# Kubera web

Next.js call UI. Setup and env vars are in the [repo README](../../README.md).

```bash
npm install
NEXT_PUBLIC_AGENT_URL=http://localhost:7860 npm run dev
npm test
```

- App: http://localhost:3000
- Board preview (no agent): http://localhost:3000/preview
